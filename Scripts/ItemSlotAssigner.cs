using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/* a class that defines the slot for each ingredient before it is dropped in the stack/queue */
public class ItemSlotAssigner : MonoBehaviour
{
    public string cellID; //stores the level of each ingredient in BFS
}
