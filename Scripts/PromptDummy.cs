using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/* a dummy class for accessing the prompt gameobject when inactive */
public class PromptDummy : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
